using System;
using System.Collections;
using System.Collections.Generic;
using System.Net.NetworkInformation;
using UnityEngine;

public class Actions : MonoBehaviour
{
 /*   private float kickCd = 0.5f;
    private bool isKicking = false;
    void punch();
    void moveLeft();
    void moveRight();
    void jump();

    Actions[] getActionDisp();
    
    void kick()
    {
        if (isKicking == false)
        {
            isKicking = true;
            
        }
    }

    private void Update()
    {
        if(kickCd <= 0)
        {
            kickCd = 0.5f;
        }

        if (isKicking == true)
        {
            kickCd -= Time.deltaTime;
        }
        
    } */
}
